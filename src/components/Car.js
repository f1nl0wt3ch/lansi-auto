import React, {Component} from 'react';

export default class Car extends Component {
    render() {
        const {car} = this.props;

        return (
            <div>

            </div>
        )
    }
}